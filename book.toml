[book]
title = "Lykn"
authors = ["Duncan McGreggor"]
description = "And Now for Something Completely Parenthetical — A Lisp Flavoured JavaScript"
language = "en"

[build]
build-dir = "book"

[preprocessor.index]

[preprocessor.links]

[preprocessor.mermaid]
command = "mdbook-mermaid"

[output.html]
no-section-label = true
default-theme = "lfe-pdp"
preferred-dark-theme = "lfe-pdp"
additional-js = ["js/mermaid.min.js", "js/mermaid-init.js"]
additional-css = ["css/custom.css"]
git-repository-url = "https://github.com/cnbbooks/lykn"

[output.html.search]
limit-results = 20
use-boolean-and = true
boost-title = 2
boost-hierarchy = 2
boost-paragraph = 1
expand = true
heading-split-level = 2

[output.html.fold]
enable = true

[output.epub]
epub-version = 3
use-default-css = false       # we ship our own, matched to the lfe-pdp theme
curly-quotes = false          # known bug with code blocks on some versions
no-section-label = true       # match HTML behaviour
cover-image = "src/images/cover.png"
additional-css = ["css/epub.css"]

# Fonts and any book-wide assets that aren't referenced from markdown.
# Chapter images (src/images/ch*.png etc.) are picked up automatically because
# they're linked from markdown; these entries are only for files that need to
# be in the EPUB package but aren't referenced from any chapter.
additional-resources = [
  "theme/fonts/RedHatDisplay-VariableFont_wght.ttf",
  "theme/fonts/RedHatDisplay-Italic-VariableFont_wght.ttf",
  "theme/fonts/Literata-VariableFont_opsz,wght.ttf",
  "theme/fonts/Literata-Italic-VariableFont_opsz,wght.ttf",
  "theme/fonts/IBMPlexMono-Regular.ttf",
  "theme/fonts/IBMPlexMono-Medium.ttf",
  "theme/fonts/IBMPlexMono-SemiBold.ttf",
  "theme/fonts/IBMPlexMono-Italic.ttf",
]

[output.pdf]
